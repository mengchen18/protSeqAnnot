# run the following line to select a fasta or dat file (download from uniprot) to annotate
# The annotation result is saved in the same folder as input file, named as [given_file_name.annot].
# To use the annotation file in MQC, copy the annotated file (.annot) to "/LIMS/AnnotDB"

# IMPORTANT: the give file path should not contain space or backslash. 
# If the file name is too long, consider making it shorter. 

source("https://raw.githubusercontent.com/mengchen18/protSeqAnnot/rstudio_local/rstudio_call.R")
